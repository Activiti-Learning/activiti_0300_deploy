when we deploy new flow to activiti. there're two types of validation conducted.


deployment.disableSchemaValidation();
deployment.disableBpmnValidation();

1. validate workflow xml file
2. workflow node is invalid. see we don't have any endpoint.
